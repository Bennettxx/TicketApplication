namespace TicketApplication.Data
{
    // ticketstatus = kanban-spalten, zahlenwerte sind im frontend fest verdrahtet
    public enum TicketStatus
    {
        Open = 0,
        InProgress = 1,
        Closed = 2
    }
}
